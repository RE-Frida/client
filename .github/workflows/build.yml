name: Build Client

on:
  push:
    branches: [main]

concurrency:
  group: client-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build (${{ matrix.os }}, ${{ matrix.profile }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact: re-frida-linux-amd64
            profile: release
            features: ""
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact: re-frida-linux-amd64-debug
            profile: debug
            features: "--features debug"
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: re-frida-windows-amd64
            profile: release
            features: ""
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: re-frida-windows-amd64-debug
            profile: debug
            features: "--features debug"

    steps:
      - uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            pkg-config \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libssl-dev \
            xz-utils

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src-tauri/target
          key: tauri-${{ runner.os }}-${{ matrix.profile }}-cargo-${{ hashFiles('src-tauri/Cargo.lock', 'src-tauri/Cargo.toml') }}
          restore-keys: |
            tauri-${{ runner.os }}-${{ matrix.profile }}-cargo-

      - name: Install frontend dependencies
        run: npm ci

      - name: Download ADB + frida (Linux)
        if: runner.os == 'Linux'
        run: |
          TRIPLE="${{ matrix.target }}"
          mkdir -p src-tauri/tools
          cd src-tauri/tools
          # ADB
          curl -sL "https://dl.google.com/android/repository/platform-tools-latest-linux.zip" -o pt.zip
          unzip -qo pt.zip
          cp platform-tools/adb "./adb-${TRIPLE}"
          chmod +x "./adb-${TRIPLE}"
          rm -rf platform-tools pt.zip
          # frida-inject
          FRIDA_TAG=$(curl -sLI -o /dev/null -w '%{url_effective}' "https://github.com/frida/frida/releases/latest" | xargs basename)
          FRIDA_VER="${FRIDA_TAG#v}"
          echo "Downloading frida-inject v${FRIDA_VER}"
          BASE_URL="https://github.com/frida/frida/releases/download/${FRIDA_TAG}"
          curl -sL "${BASE_URL}/frida-inject-${FRIDA_VER}-linux-x86_64.xz" -o frida-inject.xz
          xz -df frida-inject.xz
          mv frida-inject "frida-inject-${TRIPLE}"
          chmod +x "frida-inject-${TRIPLE}"

      - name: Download ADB + frida (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $triple = "${{ matrix.target }}"
          New-Item -ItemType Directory -Force -Path src-tauri/tools
          cd src-tauri/tools
          # ADB
          Invoke-WebRequest -Uri "https://dl.google.com/android/repository/platform-tools-latest-windows.zip" -OutFile pt.zip
          Expand-Archive -Path pt.zip -DestinationPath . -Force
          Copy-Item platform-tools/adb.exe "adb-${triple}.exe"
          Copy-Item platform-tools/AdbWinApi.dll ./AdbWinApi.dll -ErrorAction SilentlyContinue
          Copy-Item platform-tools/AdbWinUsbApi.dll ./AdbWinUsbApi.dll -ErrorAction SilentlyContinue
          Remove-Item -Recurse -Force platform-tools, pt.zip
          # frida-inject
          $apiResp = Invoke-RestMethod -Uri "https://api.github.com/repos/frida/frida/releases/latest"
          $fridaTag = $apiResp.tag_name
          $fridaVer = $fridaTag -replace '^v',''
          Write-Host "Downloading frida-inject v${fridaVer}"
          $baseUrl = "https://github.com/frida/frida/releases/download/${fridaTag}"
          & curl.exe -sL -o "frida-inject.exe.xz" "${baseUrl}/frida-inject-${fridaVer}-windows-x86_64.exe.xz"
          & xz -df "frida-inject.exe.xz"
          Rename-Item "frida-inject.exe" "frida-inject-${triple}.exe"

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        with:
          args: --target ${{ matrix.target }} ${{ matrix.features }} --bundles none
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: src-tauri/target/${{ matrix.target }}/release/re-frida-tauri*

  release:
    name: Release
    needs: [build]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Bump patch version
        id: version
        run: |
          CURRENT=$(grep '"version"' src-tauri/tauri.conf.json | head -1 | sed 's/.*"\([0-9.]*\)".*/\1/')
          IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT"
          NEW_PATCH=$((PATCH + 1))
          NEW_VERSION="${MAJOR}.${MINOR}.${NEW_PATCH}"
          echo "old=${CURRENT}" >> $GITHUB_OUTPUT
          echo "new=${NEW_VERSION}" >> $GITHUB_OUTPUT
          echo "tag=v${NEW_VERSION}" >> $GITHUB_OUTPUT
          sed -i "s/\"version\": \"${CURRENT}\"/\"version\": \"${NEW_VERSION}\"/" src-tauri/tauri.conf.json

      - name: Commit version bump
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add src-tauri/tauri.conf.json
          git commit -m "chore: bump version to ${{ steps.version.outputs.new }}" || echo "No changes"
          git tag -d ${{ steps.version.outputs.tag }} 2>/dev/null || true
          git push origin :refs/tags/${{ steps.version.outputs.tag }} 2>/dev/null || true
          git tag ${{ steps.version.outputs.tag }}
          git push origin main --tags

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: "RE:Frida Client ${{ steps.version.outputs.new }}"
          generate_release_notes: true
          files: artifacts/**/*
